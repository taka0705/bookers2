<%= render 'list',user: @user %>

<h2>Book detail</h2>
<table>
<tr>
  <td><%= image_tag @user.get_profile_image(100,100) %> <p>name <%= @user.name %></p></td>
  <td><%= link_to @book.title,user_path(@book.id)  %></td>
  <td><%= @book.body %></td>
  <td><%= link_to "Edit",edit_book_path(@book.id) %></td>
   <td><%= link_to "Destroy",edit_book_path(@book.id) %></td>
  <td></td>
  </tr>
</table>